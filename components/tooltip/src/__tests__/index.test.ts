/** @jest-environment node */

import { Tooltip } from '..';

describe('Tooltip', () => {
  it('should do something', () => {
    expect.hasAssertions();
    expect(Tooltip).toBeDefined();
    // TODO
  });
});
