'use strict'; // eslint-disable-line

// eslint-disable-next-line @typescript-eslint/no-var-requires
const { html, markup, preprocess, style } = require('@minna-ui/preprocess');

module.exports = {
  html,
  markup,
  preprocess,
  style,
};
