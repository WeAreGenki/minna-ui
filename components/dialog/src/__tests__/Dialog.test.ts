// import Dialog from '../Dialog.svelte';

describe('Dialog component', () => {
  it.todo('Write tests!');

  // it('throws error with no props', () => {
  //   expect.assertions(1);
  //   function wrapper(): void {
  //     const target = document.createElement('div');
  //     new Dialog({ target });
  //   }
  //   expect(wrapper).toThrow();
  // });

  // it('renders correctly with required props set', () => {
  //   expect.assertions(10);
  //   const target = document.createElement('div');
  //   const component = new Dialog({
  //     props: dialogOpts,
  //     target,
  //   });
  //   const dialog = target.querydialogor('.dialog');
  //   expect(Array.isArray(component.$$.ctx.items)).toEqual(true);
  //   expect(component.$$.ctx.items).not.toHaveLength(0);
  //   expect(component.$$.ctx.input).toBeDefined();
  //   expect(dialog.getAttribute('tabindex')).toEqual('0');
  //   expect(dialog.getAttribute('disabled')).toBeNull();
  //   expect(dialog.getAttribute('required')).toBeNull();
  //   expect(dialog.getAttribute('placeholder')).not.toBeFalsy();
  //   expect(document.querydialogor('dialog-active')).toBeNull();
  //   expect(document.querydialogor('dialog-disabled')).toBeNull();
  //   expect(target.innerHTML).toMatchSnapshot();
  // });
});
