// https://www.typescriptlang.org/docs/handbook/tsconfig-json.html

{
  "extends": "@minna-ui/ts-config",
  "compilerOptions": {
    "baseUrl": "."
  },
  // FIXME: `exclude` doesn't get inherited when using `extends`
  "exclude": [
    "**/.*rc.js",
    "**/*.config.js",
    "**/dist/*",
    "**/node_modules/*",
    "test/coverage"
  ]
}
